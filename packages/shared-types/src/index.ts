/**
 * @devsync/shared-types
 *
 * This is the single public barrel export for the package.
 * Re-export all type modules from here.
 *
 * Example (uncomment as modules are created):
 *   export * from './user.js';
 *   export * from './project.js';
 *   export * from './collaboration.js';
 *   export * from './ai.js';
 */

// Placeholder — replace with real exports as domain types are authored.
export const SHARED_TYPES_VERSION = '0.1.0';
